import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    width: '95%',
    alignSelf: 'center',
    marginTop: 10,
  },
  label: {
    fontSize: 16,
    fontWeight: '600',
    marginLeft: 10,
  },
});

export default styles;
